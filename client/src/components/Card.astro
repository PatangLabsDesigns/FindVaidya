---
const { cardHeading, cardBody, cardLink, cardButton } = Astro.props; 
---
 <article class="flex flex-col justify-center items-center p-9 gap-md bg-white border border-border rounded-xl text-center w-full md:w-2xl">
    <h2 class="text-2xl text-center text-dark font-bold">{cardHeading}</h2>
    <p set:html={cardBody}>.</p>
    <a href={cardLink} class="py-sm px-lg text-base rounded-lg hover:cursor-pointer bg-primary text-white hover:color-text hover:bg-primary-hover">{cardButton}</a>
 </article>